__version__ = "0.1.0"

import json
from os.path import abspath, dirname, join

repo_data_path = join(dirname(abspath(str(__file__))), "..", "repo_data")

outputs_data_path = join(dirname(abspath(str(__file__))), "..", "outputs")
